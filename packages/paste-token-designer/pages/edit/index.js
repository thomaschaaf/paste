import {EditorWrapper} from '../../components/layout/editor-wrapper';

export default function Home(props) {
  console.log(1, props);
  return <EditorWrapper>Select an category from the dropdown above to begin editing</EditorWrapper>;
}
